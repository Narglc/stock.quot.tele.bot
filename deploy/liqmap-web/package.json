{
  "name": "liqmap-web",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "dev": "wrangler dev",
    "deploy": "bash scripts/deploy.sh",
    "preview": "node scripts/preview.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.128.0"
  }
}
